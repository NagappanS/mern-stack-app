{
  "name": "food-delivery-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd Front-End/client && npm install && cd ../../Back-End && npm install",
    "build": "cd Front-End/client && npm run build",
    "start": "cd Back-End && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
